repos:
  - repo: local
    hooks:
      - id: clang-format
        name: clang-format
        entry: clang-format
        args: [--dry-run, --Werror, --style=file:.devcontainer/.clang-format]
        language: system
        files: \.(cpp|hpp|h|c|cxx|cc)$
